/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { ACCESS_CONTROL_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const INITIALIZE_ACCESS_CONTROL_DISCRIMINATOR = new Uint8Array([
  244, 90, 245, 242, 199, 224, 247, 140,
]);

export function getInitializeAccessControlDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_ACCESS_CONTROL_DISCRIMINATOR
  );
}

export type InitializeAccessControlInstruction<
  TProgram extends string = typeof ACCESS_CONTROL_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountAccessControl extends string | AccountMeta<string> = string,
  TAccountWalletRole extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    '11111111111111111111111111111111',
  TAccountTokenProgram extends string | AccountMeta<string> =
    'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountAuthority extends string
        ? ReadonlyAccount<TAccountAuthority>
        : TAccountAuthority,
      TAccountMint extends string
        ? WritableSignerAccount<TAccountMint> & AccountSignerMeta<TAccountMint>
        : TAccountMint,
      TAccountAccessControl extends string
        ? WritableAccount<TAccountAccessControl>
        : TAccountAccessControl,
      TAccountWalletRole extends string
        ? WritableAccount<TAccountWalletRole>
        : TAccountWalletRole,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeAccessControlInstructionData = {
  discriminator: ReadonlyUint8Array;
  decimals: number;
  name: string;
  symbol: string;
  uri: string;
  hookProgramId: Address;
  maxTotalSupply: bigint;
};

export type InitializeAccessControlInstructionDataArgs = {
  decimals: number;
  name: string;
  symbol: string;
  uri: string;
  hookProgramId: Address;
  maxTotalSupply: number | bigint;
};

export function getInitializeAccessControlInstructionDataEncoder(): Encoder<InitializeAccessControlInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['decimals', getU8Encoder()],
      ['name', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['symbol', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['uri', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['hookProgramId', getAddressEncoder()],
      ['maxTotalSupply', getU64Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: INITIALIZE_ACCESS_CONTROL_DISCRIMINATOR,
    })
  );
}

export function getInitializeAccessControlInstructionDataDecoder(): Decoder<InitializeAccessControlInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['decimals', getU8Decoder()],
    ['name', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['symbol', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['uri', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['hookProgramId', getAddressDecoder()],
    ['maxTotalSupply', getU64Decoder()],
  ]);
}

export function getInitializeAccessControlInstructionDataCodec(): Codec<
  InitializeAccessControlInstructionDataArgs,
  InitializeAccessControlInstructionData
> {
  return combineCodec(
    getInitializeAccessControlInstructionDataEncoder(),
    getInitializeAccessControlInstructionDataDecoder()
  );
}

export type InitializeAccessControlAsyncInput<
  TAccountPayer extends string = string,
  TAccountAuthority extends string = string,
  TAccountMint extends string = string,
  TAccountAccessControl extends string = string,
  TAccountWalletRole extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  authority: Address<TAccountAuthority>;
  mint: TransactionSigner<TAccountMint>;
  accessControl?: Address<TAccountAccessControl>;
  walletRole?: Address<TAccountWalletRole>;
  systemProgram?: Address<TAccountSystemProgram>;
  tokenProgram?: Address<TAccountTokenProgram>;
  decimals: InitializeAccessControlInstructionDataArgs['decimals'];
  name: InitializeAccessControlInstructionDataArgs['name'];
  symbol: InitializeAccessControlInstructionDataArgs['symbol'];
  uri: InitializeAccessControlInstructionDataArgs['uri'];
  hookProgramId: InitializeAccessControlInstructionDataArgs['hookProgramId'];
  maxTotalSupply: InitializeAccessControlInstructionDataArgs['maxTotalSupply'];
};

export async function getInitializeAccessControlInstructionAsync<
  TAccountPayer extends string,
  TAccountAuthority extends string,
  TAccountMint extends string,
  TAccountAccessControl extends string,
  TAccountWalletRole extends string,
  TAccountSystemProgram extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof ACCESS_CONTROL_PROGRAM_ADDRESS,
>(
  input: InitializeAccessControlAsyncInput<
    TAccountPayer,
    TAccountAuthority,
    TAccountMint,
    TAccountAccessControl,
    TAccountWalletRole,
    TAccountSystemProgram,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  InitializeAccessControlInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountAuthority,
    TAccountMint,
    TAccountAccessControl,
    TAccountWalletRole,
    TAccountSystemProgram,
    TAccountTokenProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ACCESS_CONTROL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
    mint: { value: input.mint ?? null, isWritable: true },
    accessControl: { value: input.accessControl ?? null, isWritable: true },
    walletRole: { value: input.walletRole ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.accessControl.value) {
    accounts.accessControl.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([97, 99])),
        getAddressEncoder().encode(expectAddress(accounts.mint.value)),
      ],
    });
  }
  if (!accounts.walletRole.value) {
    accounts.walletRole.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([119, 97, 108, 108, 101, 116, 95, 114, 111, 108, 101])
        ),
        getAddressEncoder().encode(expectAddress(accounts.mint.value)),
        getAddressEncoder().encode(expectAddress(accounts.payer.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb' as Address<'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.accessControl),
      getAccountMeta(accounts.walletRole),
      getAccountMeta(accounts.systemProgram),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getInitializeAccessControlInstructionDataEncoder().encode(
      args as InitializeAccessControlInstructionDataArgs
    ),
    programAddress,
  } as InitializeAccessControlInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountAuthority,
    TAccountMint,
    TAccountAccessControl,
    TAccountWalletRole,
    TAccountSystemProgram,
    TAccountTokenProgram
  >);
}

export type InitializeAccessControlInput<
  TAccountPayer extends string = string,
  TAccountAuthority extends string = string,
  TAccountMint extends string = string,
  TAccountAccessControl extends string = string,
  TAccountWalletRole extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  authority: Address<TAccountAuthority>;
  mint: TransactionSigner<TAccountMint>;
  accessControl: Address<TAccountAccessControl>;
  walletRole: Address<TAccountWalletRole>;
  systemProgram?: Address<TAccountSystemProgram>;
  tokenProgram?: Address<TAccountTokenProgram>;
  decimals: InitializeAccessControlInstructionDataArgs['decimals'];
  name: InitializeAccessControlInstructionDataArgs['name'];
  symbol: InitializeAccessControlInstructionDataArgs['symbol'];
  uri: InitializeAccessControlInstructionDataArgs['uri'];
  hookProgramId: InitializeAccessControlInstructionDataArgs['hookProgramId'];
  maxTotalSupply: InitializeAccessControlInstructionDataArgs['maxTotalSupply'];
};

export function getInitializeAccessControlInstruction<
  TAccountPayer extends string,
  TAccountAuthority extends string,
  TAccountMint extends string,
  TAccountAccessControl extends string,
  TAccountWalletRole extends string,
  TAccountSystemProgram extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof ACCESS_CONTROL_PROGRAM_ADDRESS,
>(
  input: InitializeAccessControlInput<
    TAccountPayer,
    TAccountAuthority,
    TAccountMint,
    TAccountAccessControl,
    TAccountWalletRole,
    TAccountSystemProgram,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress }
): InitializeAccessControlInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountAuthority,
  TAccountMint,
  TAccountAccessControl,
  TAccountWalletRole,
  TAccountSystemProgram,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ACCESS_CONTROL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
    mint: { value: input.mint ?? null, isWritable: true },
    accessControl: { value: input.accessControl ?? null, isWritable: true },
    walletRole: { value: input.walletRole ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb' as Address<'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.accessControl),
      getAccountMeta(accounts.walletRole),
      getAccountMeta(accounts.systemProgram),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getInitializeAccessControlInstructionDataEncoder().encode(
      args as InitializeAccessControlInstructionDataArgs
    ),
    programAddress,
  } as InitializeAccessControlInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountAuthority,
    TAccountMint,
    TAccountAccessControl,
    TAccountWalletRole,
    TAccountSystemProgram,
    TAccountTokenProgram
  >);
}

export type ParsedInitializeAccessControlInstruction<
  TProgram extends string = typeof ACCESS_CONTROL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    authority: TAccountMetas[1];
    mint: TAccountMetas[2];
    accessControl: TAccountMetas[3];
    walletRole: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
    tokenProgram: TAccountMetas[6];
  };
  data: InitializeAccessControlInstructionData;
};

export function parseInitializeAccessControlInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedInitializeAccessControlInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      authority: getNextAccount(),
      mint: getNextAccount(),
      accessControl: getNextAccount(),
      walletRole: getNextAccount(),
      systemProgram: getNextAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getInitializeAccessControlInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
