/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { DIVIDENDS_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const ACCEPT_RECLAIMER_OWNERSHIP_DISCRIMINATOR = new Uint8Array([
  211, 214, 76, 233, 74, 143, 206, 28,
]);

export function getAcceptReclaimerOwnershipDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ACCEPT_RECLAIMER_OWNERSHIP_DISCRIMINATOR
  );
}

export type AcceptReclaimerOwnershipInstruction<
  TProgram extends string = typeof DIVIDENDS_PROGRAM_ADDRESS,
  TAccountReclaimer extends string | AccountMeta<string> = string,
  TAccountAccessControl extends string | AccountMeta<string> = string,
  TAccountSecurityMint extends string | AccountMeta<string> = string,
  TAccountNewOwner extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountReclaimer extends string
        ? WritableAccount<TAccountReclaimer>
        : TAccountReclaimer,
      TAccountAccessControl extends string
        ? ReadonlyAccount<TAccountAccessControl>
        : TAccountAccessControl,
      TAccountSecurityMint extends string
        ? ReadonlyAccount<TAccountSecurityMint>
        : TAccountSecurityMint,
      TAccountNewOwner extends string
        ? ReadonlySignerAccount<TAccountNewOwner> &
            AccountSignerMeta<TAccountNewOwner>
        : TAccountNewOwner,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type AcceptReclaimerOwnershipInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type AcceptReclaimerOwnershipInstructionDataArgs = {};

export function getAcceptReclaimerOwnershipInstructionDataEncoder(): FixedSizeEncoder<AcceptReclaimerOwnershipInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: ACCEPT_RECLAIMER_OWNERSHIP_DISCRIMINATOR,
    })
  );
}

export function getAcceptReclaimerOwnershipInstructionDataDecoder(): FixedSizeDecoder<AcceptReclaimerOwnershipInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getAcceptReclaimerOwnershipInstructionDataCodec(): FixedSizeCodec<
  AcceptReclaimerOwnershipInstructionDataArgs,
  AcceptReclaimerOwnershipInstructionData
> {
  return combineCodec(
    getAcceptReclaimerOwnershipInstructionDataEncoder(),
    getAcceptReclaimerOwnershipInstructionDataDecoder()
  );
}

export type AcceptReclaimerOwnershipAsyncInput<
  TAccountReclaimer extends string = string,
  TAccountAccessControl extends string = string,
  TAccountSecurityMint extends string = string,
  TAccountNewOwner extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Reclaimer account storing the wallet address. */
  reclaimer?: Address<TAccountReclaimer>;
  /** Access Control for Security Token. */
  accessControl?: Address<TAccountAccessControl>;
  securityMint: Address<TAccountSecurityMint>;
  /** New owner signing the transaction (must match proposed_wallet_address). */
  newOwner: TransactionSigner<TAccountNewOwner>;
  /** The [System] program. */
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getAcceptReclaimerOwnershipInstructionAsync<
  TAccountReclaimer extends string,
  TAccountAccessControl extends string,
  TAccountSecurityMint extends string,
  TAccountNewOwner extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof DIVIDENDS_PROGRAM_ADDRESS,
>(
  input: AcceptReclaimerOwnershipAsyncInput<
    TAccountReclaimer,
    TAccountAccessControl,
    TAccountSecurityMint,
    TAccountNewOwner,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  AcceptReclaimerOwnershipInstruction<
    TProgramAddress,
    TAccountReclaimer,
    TAccountAccessControl,
    TAccountSecurityMint,
    TAccountNewOwner,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? DIVIDENDS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    reclaimer: { value: input.reclaimer ?? null, isWritable: true },
    accessControl: { value: input.accessControl ?? null, isWritable: false },
    securityMint: { value: input.securityMint ?? null, isWritable: false },
    newOwner: { value: input.newOwner ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.accessControl.value) {
    accounts.accessControl.value = await getProgramDerivedAddress({
      programAddress:
        '4X79YRjz9KNMhdjdxXg2ZNTS3YnMGYdwJkBHnezMJwr3' as Address<'4X79YRjz9KNMhdjdxXg2ZNTS3YnMGYdwJkBHnezMJwr3'>,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([97, 99])),
        getAddressEncoder().encode(expectAddress(accounts.securityMint.value)),
      ],
    });
  }
  if (!accounts.reclaimer.value) {
    accounts.reclaimer.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([114, 101, 99, 108, 97, 105, 109, 101, 114])
        ),
        getAddressEncoder().encode(expectAddress(accounts.accessControl.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.reclaimer),
      getAccountMeta(accounts.accessControl),
      getAccountMeta(accounts.securityMint),
      getAccountMeta(accounts.newOwner),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getAcceptReclaimerOwnershipInstructionDataEncoder().encode({}),
    programAddress,
  } as AcceptReclaimerOwnershipInstruction<
    TProgramAddress,
    TAccountReclaimer,
    TAccountAccessControl,
    TAccountSecurityMint,
    TAccountNewOwner,
    TAccountSystemProgram
  >);
}

export type AcceptReclaimerOwnershipInput<
  TAccountReclaimer extends string = string,
  TAccountAccessControl extends string = string,
  TAccountSecurityMint extends string = string,
  TAccountNewOwner extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Reclaimer account storing the wallet address. */
  reclaimer: Address<TAccountReclaimer>;
  /** Access Control for Security Token. */
  accessControl: Address<TAccountAccessControl>;
  securityMint: Address<TAccountSecurityMint>;
  /** New owner signing the transaction (must match proposed_wallet_address). */
  newOwner: TransactionSigner<TAccountNewOwner>;
  /** The [System] program. */
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getAcceptReclaimerOwnershipInstruction<
  TAccountReclaimer extends string,
  TAccountAccessControl extends string,
  TAccountSecurityMint extends string,
  TAccountNewOwner extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof DIVIDENDS_PROGRAM_ADDRESS,
>(
  input: AcceptReclaimerOwnershipInput<
    TAccountReclaimer,
    TAccountAccessControl,
    TAccountSecurityMint,
    TAccountNewOwner,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): AcceptReclaimerOwnershipInstruction<
  TProgramAddress,
  TAccountReclaimer,
  TAccountAccessControl,
  TAccountSecurityMint,
  TAccountNewOwner,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? DIVIDENDS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    reclaimer: { value: input.reclaimer ?? null, isWritable: true },
    accessControl: { value: input.accessControl ?? null, isWritable: false },
    securityMint: { value: input.securityMint ?? null, isWritable: false },
    newOwner: { value: input.newOwner ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.reclaimer),
      getAccountMeta(accounts.accessControl),
      getAccountMeta(accounts.securityMint),
      getAccountMeta(accounts.newOwner),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getAcceptReclaimerOwnershipInstructionDataEncoder().encode({}),
    programAddress,
  } as AcceptReclaimerOwnershipInstruction<
    TProgramAddress,
    TAccountReclaimer,
    TAccountAccessControl,
    TAccountSecurityMint,
    TAccountNewOwner,
    TAccountSystemProgram
  >);
}

export type ParsedAcceptReclaimerOwnershipInstruction<
  TProgram extends string = typeof DIVIDENDS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Reclaimer account storing the wallet address. */
    reclaimer: TAccountMetas[0];
    /** Access Control for Security Token. */
    accessControl: TAccountMetas[1];
    securityMint: TAccountMetas[2];
    /** New owner signing the transaction (must match proposed_wallet_address). */
    newOwner: TAccountMetas[3];
    /** The [System] program. */
    systemProgram: TAccountMetas[4];
  };
  data: AcceptReclaimerOwnershipInstructionData;
};

export function parseAcceptReclaimerOwnershipInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedAcceptReclaimerOwnershipInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      reclaimer: getNextAccount(),
      accessControl: getNextAccount(),
      securityMint: getNextAccount(),
      newOwner: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getAcceptReclaimerOwnershipInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
