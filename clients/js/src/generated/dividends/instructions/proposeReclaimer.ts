/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { DIVIDENDS_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const PROPOSE_RECLAIMER_DISCRIMINATOR = new Uint8Array([
  191, 148, 66, 41, 132, 154, 25, 208,
]);

export function getProposeReclaimerDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PROPOSE_RECLAIMER_DISCRIMINATOR
  );
}

export type ProposeReclaimerInstruction<
  TProgram extends string = typeof DIVIDENDS_PROGRAM_ADDRESS,
  TAccountReclaimer extends string | AccountMeta<string> = string,
  TAccountAccessControl extends string | AccountMeta<string> = string,
  TAccountAuthorityWalletRole extends string | AccountMeta<string> = string,
  TAccountSecurityMint extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountReclaimer extends string
        ? WritableAccount<TAccountReclaimer>
        : TAccountReclaimer,
      TAccountAccessControl extends string
        ? ReadonlyAccount<TAccountAccessControl>
        : TAccountAccessControl,
      TAccountAuthorityWalletRole extends string
        ? ReadonlyAccount<TAccountAuthorityWalletRole>
        : TAccountAuthorityWalletRole,
      TAccountSecurityMint extends string
        ? ReadonlyAccount<TAccountSecurityMint>
        : TAccountSecurityMint,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ProposeReclaimerInstructionData = {
  discriminator: ReadonlyUint8Array;
  newReclaimerWallet: Address;
};

export type ProposeReclaimerInstructionDataArgs = {
  newReclaimerWallet: Address;
};

export function getProposeReclaimerInstructionDataEncoder(): FixedSizeEncoder<ProposeReclaimerInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['newReclaimerWallet', getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: PROPOSE_RECLAIMER_DISCRIMINATOR })
  );
}

export function getProposeReclaimerInstructionDataDecoder(): FixedSizeDecoder<ProposeReclaimerInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['newReclaimerWallet', getAddressDecoder()],
  ]);
}

export function getProposeReclaimerInstructionDataCodec(): FixedSizeCodec<
  ProposeReclaimerInstructionDataArgs,
  ProposeReclaimerInstructionData
> {
  return combineCodec(
    getProposeReclaimerInstructionDataEncoder(),
    getProposeReclaimerInstructionDataDecoder()
  );
}

export type ProposeReclaimerAsyncInput<
  TAccountReclaimer extends string = string,
  TAccountAccessControl extends string = string,
  TAccountAuthorityWalletRole extends string = string,
  TAccountSecurityMint extends string = string,
  TAccountAuthority extends string = string,
  TAccountPayer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Reclaimer account storing the wallet address. */
  reclaimer?: Address<TAccountReclaimer>;
  /** Access Control for Security Token. */
  accessControl?: Address<TAccountAccessControl>;
  /** Authority wallet role to propose the reclaimer. */
  authorityWalletRole: Address<TAccountAuthorityWalletRole>;
  securityMint: Address<TAccountSecurityMint>;
  /** Authority signing the transaction. */
  authority: TransactionSigner<TAccountAuthority>;
  /** Payer for the reclaimer account initialization. */
  payer: TransactionSigner<TAccountPayer>;
  /** The [System] program. */
  systemProgram?: Address<TAccountSystemProgram>;
  newReclaimerWallet: ProposeReclaimerInstructionDataArgs['newReclaimerWallet'];
};

export async function getProposeReclaimerInstructionAsync<
  TAccountReclaimer extends string,
  TAccountAccessControl extends string,
  TAccountAuthorityWalletRole extends string,
  TAccountSecurityMint extends string,
  TAccountAuthority extends string,
  TAccountPayer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof DIVIDENDS_PROGRAM_ADDRESS,
>(
  input: ProposeReclaimerAsyncInput<
    TAccountReclaimer,
    TAccountAccessControl,
    TAccountAuthorityWalletRole,
    TAccountSecurityMint,
    TAccountAuthority,
    TAccountPayer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  ProposeReclaimerInstruction<
    TProgramAddress,
    TAccountReclaimer,
    TAccountAccessControl,
    TAccountAuthorityWalletRole,
    TAccountSecurityMint,
    TAccountAuthority,
    TAccountPayer,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? DIVIDENDS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    reclaimer: { value: input.reclaimer ?? null, isWritable: true },
    accessControl: { value: input.accessControl ?? null, isWritable: false },
    authorityWalletRole: {
      value: input.authorityWalletRole ?? null,
      isWritable: false,
    },
    securityMint: { value: input.securityMint ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: false },
    payer: { value: input.payer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.accessControl.value) {
    accounts.accessControl.value = await getProgramDerivedAddress({
      programAddress:
        '4X79YRjz9KNMhdjdxXg2ZNTS3YnMGYdwJkBHnezMJwr3' as Address<'4X79YRjz9KNMhdjdxXg2ZNTS3YnMGYdwJkBHnezMJwr3'>,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([97, 99])),
        getAddressEncoder().encode(expectAddress(accounts.securityMint.value)),
      ],
    });
  }
  if (!accounts.reclaimer.value) {
    accounts.reclaimer.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([114, 101, 99, 108, 97, 105, 109, 101, 114])
        ),
        getAddressEncoder().encode(expectAddress(accounts.accessControl.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.reclaimer),
      getAccountMeta(accounts.accessControl),
      getAccountMeta(accounts.authorityWalletRole),
      getAccountMeta(accounts.securityMint),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getProposeReclaimerInstructionDataEncoder().encode(
      args as ProposeReclaimerInstructionDataArgs
    ),
    programAddress,
  } as ProposeReclaimerInstruction<
    TProgramAddress,
    TAccountReclaimer,
    TAccountAccessControl,
    TAccountAuthorityWalletRole,
    TAccountSecurityMint,
    TAccountAuthority,
    TAccountPayer,
    TAccountSystemProgram
  >);
}

export type ProposeReclaimerInput<
  TAccountReclaimer extends string = string,
  TAccountAccessControl extends string = string,
  TAccountAuthorityWalletRole extends string = string,
  TAccountSecurityMint extends string = string,
  TAccountAuthority extends string = string,
  TAccountPayer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Reclaimer account storing the wallet address. */
  reclaimer: Address<TAccountReclaimer>;
  /** Access Control for Security Token. */
  accessControl: Address<TAccountAccessControl>;
  /** Authority wallet role to propose the reclaimer. */
  authorityWalletRole: Address<TAccountAuthorityWalletRole>;
  securityMint: Address<TAccountSecurityMint>;
  /** Authority signing the transaction. */
  authority: TransactionSigner<TAccountAuthority>;
  /** Payer for the reclaimer account initialization. */
  payer: TransactionSigner<TAccountPayer>;
  /** The [System] program. */
  systemProgram?: Address<TAccountSystemProgram>;
  newReclaimerWallet: ProposeReclaimerInstructionDataArgs['newReclaimerWallet'];
};

export function getProposeReclaimerInstruction<
  TAccountReclaimer extends string,
  TAccountAccessControl extends string,
  TAccountAuthorityWalletRole extends string,
  TAccountSecurityMint extends string,
  TAccountAuthority extends string,
  TAccountPayer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof DIVIDENDS_PROGRAM_ADDRESS,
>(
  input: ProposeReclaimerInput<
    TAccountReclaimer,
    TAccountAccessControl,
    TAccountAuthorityWalletRole,
    TAccountSecurityMint,
    TAccountAuthority,
    TAccountPayer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): ProposeReclaimerInstruction<
  TProgramAddress,
  TAccountReclaimer,
  TAccountAccessControl,
  TAccountAuthorityWalletRole,
  TAccountSecurityMint,
  TAccountAuthority,
  TAccountPayer,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? DIVIDENDS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    reclaimer: { value: input.reclaimer ?? null, isWritable: true },
    accessControl: { value: input.accessControl ?? null, isWritable: false },
    authorityWalletRole: {
      value: input.authorityWalletRole ?? null,
      isWritable: false,
    },
    securityMint: { value: input.securityMint ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: false },
    payer: { value: input.payer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.reclaimer),
      getAccountMeta(accounts.accessControl),
      getAccountMeta(accounts.authorityWalletRole),
      getAccountMeta(accounts.securityMint),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getProposeReclaimerInstructionDataEncoder().encode(
      args as ProposeReclaimerInstructionDataArgs
    ),
    programAddress,
  } as ProposeReclaimerInstruction<
    TProgramAddress,
    TAccountReclaimer,
    TAccountAccessControl,
    TAccountAuthorityWalletRole,
    TAccountSecurityMint,
    TAccountAuthority,
    TAccountPayer,
    TAccountSystemProgram
  >);
}

export type ParsedProposeReclaimerInstruction<
  TProgram extends string = typeof DIVIDENDS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Reclaimer account storing the wallet address. */
    reclaimer: TAccountMetas[0];
    /** Access Control for Security Token. */
    accessControl: TAccountMetas[1];
    /** Authority wallet role to propose the reclaimer. */
    authorityWalletRole: TAccountMetas[2];
    securityMint: TAccountMetas[3];
    /** Authority signing the transaction. */
    authority: TAccountMetas[4];
    /** Payer for the reclaimer account initialization. */
    payer: TAccountMetas[5];
    /** The [System] program. */
    systemProgram: TAccountMetas[6];
  };
  data: ProposeReclaimerInstructionData;
};

export function parseProposeReclaimerInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedProposeReclaimerInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      reclaimer: getNextAccount(),
      accessControl: getNextAccount(),
      authorityWalletRole: getNextAccount(),
      securityMint: getNextAccount(),
      authority: getNextAccount(),
      payer: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getProposeReclaimerInstructionDataDecoder().decode(instruction.data),
  };
}
