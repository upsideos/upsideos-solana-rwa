/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { DIVIDENDS_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const FUND_DIVIDENDS_DISCRIMINATOR = new Uint8Array([
  80, 231, 140, 123, 85, 15, 70, 166,
]);

export function getFundDividendsDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    FUND_DIVIDENDS_DISCRIMINATOR
  );
}

export type FundDividendsInstruction<
  TProgram extends string = typeof DIVIDENDS_PROGRAM_ADDRESS,
  TAccountDistributor extends string | AccountMeta<string> = string,
  TAccountFrom extends string | AccountMeta<string> = string,
  TAccountTo extends string | AccountMeta<string> = string,
  TAccountFunder extends string | AccountMeta<string> = string,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountDistributor extends string
        ? WritableAccount<TAccountDistributor>
        : TAccountDistributor,
      TAccountFrom extends string
        ? WritableAccount<TAccountFrom>
        : TAccountFrom,
      TAccountTo extends string ? WritableAccount<TAccountTo> : TAccountTo,
      TAccountFunder extends string
        ? ReadonlySignerAccount<TAccountFunder> &
            AccountSignerMeta<TAccountFunder>
        : TAccountFunder,
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountMint extends string
        ? ReadonlyAccount<TAccountMint>
        : TAccountMint,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type FundDividendsInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
};

export type FundDividendsInstructionDataArgs = { amount: number | bigint };

export function getFundDividendsInstructionDataEncoder(): FixedSizeEncoder<FundDividendsInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['amount', getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: FUND_DIVIDENDS_DISCRIMINATOR })
  );
}

export function getFundDividendsInstructionDataDecoder(): FixedSizeDecoder<FundDividendsInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['amount', getU64Decoder()],
  ]);
}

export function getFundDividendsInstructionDataCodec(): FixedSizeCodec<
  FundDividendsInstructionDataArgs,
  FundDividendsInstructionData
> {
  return combineCodec(
    getFundDividendsInstructionDataEncoder(),
    getFundDividendsInstructionDataDecoder()
  );
}

export type FundDividendsInput<
  TAccountDistributor extends string = string,
  TAccountFrom extends string = string,
  TAccountTo extends string = string,
  TAccountFunder extends string = string,
  TAccountPayer extends string = string,
  TAccountMint extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  /** The [MerkleDistributor]. */
  distributor: Address<TAccountDistributor>;
  /** Account which send the funding tokens. */
  from: Address<TAccountFrom>;
  /** Distributor ATA containing the tokens to distribute. */
  to: Address<TAccountTo>;
  /** Who is funding the tokens. */
  funder: TransactionSigner<TAccountFunder>;
  /** Payer of the fund dividends. */
  payer: TransactionSigner<TAccountPayer>;
  mint: Address<TAccountMint>;
  /** SPL [Token] program. */
  tokenProgram?: Address<TAccountTokenProgram>;
  amount: FundDividendsInstructionDataArgs['amount'];
};

export function getFundDividendsInstruction<
  TAccountDistributor extends string,
  TAccountFrom extends string,
  TAccountTo extends string,
  TAccountFunder extends string,
  TAccountPayer extends string,
  TAccountMint extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof DIVIDENDS_PROGRAM_ADDRESS,
>(
  input: FundDividendsInput<
    TAccountDistributor,
    TAccountFrom,
    TAccountTo,
    TAccountFunder,
    TAccountPayer,
    TAccountMint,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress }
): FundDividendsInstruction<
  TProgramAddress,
  TAccountDistributor,
  TAccountFrom,
  TAccountTo,
  TAccountFunder,
  TAccountPayer,
  TAccountMint,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? DIVIDENDS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    distributor: { value: input.distributor ?? null, isWritable: true },
    from: { value: input.from ?? null, isWritable: true },
    to: { value: input.to ?? null, isWritable: true },
    funder: { value: input.funder ?? null, isWritable: false },
    payer: { value: input.payer ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA' as Address<'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.distributor),
      getAccountMeta(accounts.from),
      getAccountMeta(accounts.to),
      getAccountMeta(accounts.funder),
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getFundDividendsInstructionDataEncoder().encode(
      args as FundDividendsInstructionDataArgs
    ),
    programAddress,
  } as FundDividendsInstruction<
    TProgramAddress,
    TAccountDistributor,
    TAccountFrom,
    TAccountTo,
    TAccountFunder,
    TAccountPayer,
    TAccountMint,
    TAccountTokenProgram
  >);
}

export type ParsedFundDividendsInstruction<
  TProgram extends string = typeof DIVIDENDS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The [MerkleDistributor]. */
    distributor: TAccountMetas[0];
    /** Account which send the funding tokens. */
    from: TAccountMetas[1];
    /** Distributor ATA containing the tokens to distribute. */
    to: TAccountMetas[2];
    /** Who is funding the tokens. */
    funder: TAccountMetas[3];
    /** Payer of the fund dividends. */
    payer: TAccountMetas[4];
    mint: TAccountMetas[5];
    /** SPL [Token] program. */
    tokenProgram: TAccountMetas[6];
  };
  data: FundDividendsInstructionData;
};

export function parseFundDividendsInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedFundDividendsInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      distributor: getNextAccount(),
      from: getNextAccount(),
      to: getNextAccount(),
      funder: getNextAccount(),
      payer: getNextAccount(),
      mint: getNextAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getFundDividendsInstructionDataDecoder().decode(instruction.data),
  };
}
