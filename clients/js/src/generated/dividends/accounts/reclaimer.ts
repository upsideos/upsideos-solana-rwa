/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from '@solana/kit';

export const RECLAIMER_DISCRIMINATOR = new Uint8Array([
  191, 103, 233, 218, 91, 3, 158, 191,
]);

export function getReclaimerDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(RECLAIMER_DISCRIMINATOR);
}

export type Reclaimer = {
  discriminator: ReadonlyUint8Array;
  /** Wallet address that will receive reclaimed dividends. */
  walletAddress: Address;
  /** Proposed new wallet address during ownership transfer */
  proposedWalletAddress: Option<Address>;
};

export type ReclaimerArgs = {
  /** Wallet address that will receive reclaimed dividends. */
  walletAddress: Address;
  /** Proposed new wallet address during ownership transfer */
  proposedWalletAddress: OptionOrNullable<Address>;
};

/** Gets the encoder for {@link ReclaimerArgs} account data. */
export function getReclaimerEncoder(): Encoder<ReclaimerArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['walletAddress', getAddressEncoder()],
      ['proposedWalletAddress', getOptionEncoder(getAddressEncoder())],
    ]),
    (value) => ({ ...value, discriminator: RECLAIMER_DISCRIMINATOR })
  );
}

/** Gets the decoder for {@link Reclaimer} account data. */
export function getReclaimerDecoder(): Decoder<Reclaimer> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['walletAddress', getAddressDecoder()],
    ['proposedWalletAddress', getOptionDecoder(getAddressDecoder())],
  ]);
}

/** Gets the codec for {@link Reclaimer} account data. */
export function getReclaimerCodec(): Codec<ReclaimerArgs, Reclaimer> {
  return combineCodec(getReclaimerEncoder(), getReclaimerDecoder());
}

export function decodeReclaimer<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>
): Account<Reclaimer, TAddress>;
export function decodeReclaimer<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>
): MaybeAccount<Reclaimer, TAddress>;
export function decodeReclaimer<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>
): Account<Reclaimer, TAddress> | MaybeAccount<Reclaimer, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getReclaimerDecoder()
  );
}

export async function fetchReclaimer<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<Account<Reclaimer, TAddress>> {
  const maybeAccount = await fetchMaybeReclaimer(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeReclaimer<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<MaybeAccount<Reclaimer, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeReclaimer(maybeAccount);
}

export async function fetchAllReclaimer(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<Account<Reclaimer>[]> {
  const maybeAccounts = await fetchAllMaybeReclaimer(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeReclaimer(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<MaybeAccount<Reclaimer>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeReclaimer(maybeAccount));
}
