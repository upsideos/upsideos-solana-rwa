/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from '@solana/kit';

export const HOLDER_GROUP_DISCRIMINATOR = new Uint8Array([
  136, 231, 252, 48, 92, 187, 25, 164,
]);

export function getHolderGroupDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    HOLDER_GROUP_DISCRIMINATOR
  );
}

export type HolderGroup = {
  discriminator: ReadonlyUint8Array;
  group: bigint;
  holder: Address;
  currentWalletsCount: bigint;
};

export type HolderGroupArgs = {
  group: number | bigint;
  holder: Address;
  currentWalletsCount: number | bigint;
};

/** Gets the encoder for {@link HolderGroupArgs} account data. */
export function getHolderGroupEncoder(): FixedSizeEncoder<HolderGroupArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['group', getU64Encoder()],
      ['holder', getAddressEncoder()],
      ['currentWalletsCount', getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: HOLDER_GROUP_DISCRIMINATOR })
  );
}

/** Gets the decoder for {@link HolderGroup} account data. */
export function getHolderGroupDecoder(): FixedSizeDecoder<HolderGroup> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['group', getU64Decoder()],
    ['holder', getAddressDecoder()],
    ['currentWalletsCount', getU64Decoder()],
  ]);
}

/** Gets the codec for {@link HolderGroup} account data. */
export function getHolderGroupCodec(): FixedSizeCodec<
  HolderGroupArgs,
  HolderGroup
> {
  return combineCodec(getHolderGroupEncoder(), getHolderGroupDecoder());
}

export function decodeHolderGroup<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>
): Account<HolderGroup, TAddress>;
export function decodeHolderGroup<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>
): MaybeAccount<HolderGroup, TAddress>;
export function decodeHolderGroup<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>
): Account<HolderGroup, TAddress> | MaybeAccount<HolderGroup, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getHolderGroupDecoder()
  );
}

export async function fetchHolderGroup<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<Account<HolderGroup, TAddress>> {
  const maybeAccount = await fetchMaybeHolderGroup(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeHolderGroup<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<MaybeAccount<HolderGroup, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeHolderGroup(maybeAccount);
}

export async function fetchAllHolderGroup(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<Account<HolderGroup>[]> {
  const maybeAccounts = await fetchAllMaybeHolderGroup(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeHolderGroup(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<MaybeAccount<HolderGroup>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeHolderGroup(maybeAccount));
}

export function getHolderGroupSize(): number {
  return 56;
}
