//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;

pub const FUND_DIVIDENDS_DISCRIMINATOR: [u8; 8] = [80, 231, 140, 123, 85, 15, 70, 166];

/// Accounts.
#[derive(Debug)]
pub struct FundDividends {
    /// The [MerkleDistributor].
    pub distributor: solana_pubkey::Pubkey,
    /// Account which send the funding tokens.
    pub from: solana_pubkey::Pubkey,
    /// Distributor ATA containing the tokens to distribute.
    pub to: solana_pubkey::Pubkey,
    /// Who is funding the tokens.
    pub funder: solana_pubkey::Pubkey,
    /// Payer of the fund dividends.
    pub payer: solana_pubkey::Pubkey,

    pub mint: solana_pubkey::Pubkey,
    /// SPL [Token] program.
    pub token_program: solana_pubkey::Pubkey,
}

impl FundDividends {
    pub fn instruction(
        &self,
        args: FundDividendsInstructionArgs,
    ) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: FundDividendsInstructionArgs,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(7 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(
            self.distributor,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(self.from, false));
        accounts.push(solana_instruction::AccountMeta::new(self.to, false));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.funder,
            true,
        ));
        accounts.push(solana_instruction::AccountMeta::new(self.payer, true));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.mint, false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.token_program,
            false,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let mut data = FundDividendsInstructionData::new().try_to_vec().unwrap();
        let mut args = args.try_to_vec().unwrap();
        data.append(&mut args);

        solana_instruction::Instruction {
            program_id: crate::DIVIDENDS_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct FundDividendsInstructionData {
    discriminator: [u8; 8],
}

impl FundDividendsInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [80, 231, 140, 123, 85, 15, 70, 166],
        }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for FundDividendsInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct FundDividendsInstructionArgs {
    pub amount: u64,
}

impl FundDividendsInstructionArgs {
    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

/// Instruction builder for `FundDividends`.
///
/// ### Accounts:
///
///   0. `[writable]` distributor
///   1. `[writable]` from
///   2. `[writable]` to
///   3. `[signer]` funder
///   4. `[writable, signer]` payer
///   5. `[]` mint
///   6. `[optional]` token_program (default to `TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA`)
#[derive(Clone, Debug, Default)]
pub struct FundDividendsBuilder {
    distributor: Option<solana_pubkey::Pubkey>,
    from: Option<solana_pubkey::Pubkey>,
    to: Option<solana_pubkey::Pubkey>,
    funder: Option<solana_pubkey::Pubkey>,
    payer: Option<solana_pubkey::Pubkey>,
    mint: Option<solana_pubkey::Pubkey>,
    token_program: Option<solana_pubkey::Pubkey>,
    amount: Option<u64>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl FundDividendsBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    /// The [MerkleDistributor].
    #[inline(always)]
    pub fn distributor(&mut self, distributor: solana_pubkey::Pubkey) -> &mut Self {
        self.distributor = Some(distributor);
        self
    }
    /// Account which send the funding tokens.
    #[inline(always)]
    pub fn from(&mut self, from: solana_pubkey::Pubkey) -> &mut Self {
        self.from = Some(from);
        self
    }
    /// Distributor ATA containing the tokens to distribute.
    #[inline(always)]
    pub fn to(&mut self, to: solana_pubkey::Pubkey) -> &mut Self {
        self.to = Some(to);
        self
    }
    /// Who is funding the tokens.
    #[inline(always)]
    pub fn funder(&mut self, funder: solana_pubkey::Pubkey) -> &mut Self {
        self.funder = Some(funder);
        self
    }
    /// Payer of the fund dividends.
    #[inline(always)]
    pub fn payer(&mut self, payer: solana_pubkey::Pubkey) -> &mut Self {
        self.payer = Some(payer);
        self
    }
    #[inline(always)]
    pub fn mint(&mut self, mint: solana_pubkey::Pubkey) -> &mut Self {
        self.mint = Some(mint);
        self
    }
    /// `[optional account, default to 'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA']`
    /// SPL [Token] program.
    #[inline(always)]
    pub fn token_program(&mut self, token_program: solana_pubkey::Pubkey) -> &mut Self {
        self.token_program = Some(token_program);
        self
    }
    #[inline(always)]
    pub fn amount(&mut self, amount: u64) -> &mut Self {
        self.amount = Some(amount);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = FundDividends {
            distributor: self.distributor.expect("distributor is not set"),
            from: self.from.expect("from is not set"),
            to: self.to.expect("to is not set"),
            funder: self.funder.expect("funder is not set"),
            payer: self.payer.expect("payer is not set"),
            mint: self.mint.expect("mint is not set"),
            token_program: self.token_program.unwrap_or(solana_pubkey::pubkey!(
                "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"
            )),
        };
        let args = FundDividendsInstructionArgs {
            amount: self.amount.clone().expect("amount is not set"),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `fund_dividends` CPI accounts.
pub struct FundDividendsCpiAccounts<'a, 'b> {
    /// The [MerkleDistributor].
    pub distributor: &'b solana_account_info::AccountInfo<'a>,
    /// Account which send the funding tokens.
    pub from: &'b solana_account_info::AccountInfo<'a>,
    /// Distributor ATA containing the tokens to distribute.
    pub to: &'b solana_account_info::AccountInfo<'a>,
    /// Who is funding the tokens.
    pub funder: &'b solana_account_info::AccountInfo<'a>,
    /// Payer of the fund dividends.
    pub payer: &'b solana_account_info::AccountInfo<'a>,

    pub mint: &'b solana_account_info::AccountInfo<'a>,
    /// SPL [Token] program.
    pub token_program: &'b solana_account_info::AccountInfo<'a>,
}

/// `fund_dividends` CPI instruction.
pub struct FundDividendsCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,
    /// The [MerkleDistributor].
    pub distributor: &'b solana_account_info::AccountInfo<'a>,
    /// Account which send the funding tokens.
    pub from: &'b solana_account_info::AccountInfo<'a>,
    /// Distributor ATA containing the tokens to distribute.
    pub to: &'b solana_account_info::AccountInfo<'a>,
    /// Who is funding the tokens.
    pub funder: &'b solana_account_info::AccountInfo<'a>,
    /// Payer of the fund dividends.
    pub payer: &'b solana_account_info::AccountInfo<'a>,

    pub mint: &'b solana_account_info::AccountInfo<'a>,
    /// SPL [Token] program.
    pub token_program: &'b solana_account_info::AccountInfo<'a>,
    /// The arguments for the instruction.
    pub __args: FundDividendsInstructionArgs,
}

impl<'a, 'b> FundDividendsCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: FundDividendsCpiAccounts<'a, 'b>,
        args: FundDividendsInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            distributor: accounts.distributor,
            from: accounts.from,
            to: accounts.to,
            funder: accounts.funder,
            payer: accounts.payer,
            mint: accounts.mint,
            token_program: accounts.token_program,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(7 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(
            *self.distributor.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(*self.from.key, false));
        accounts.push(solana_instruction::AccountMeta::new(*self.to.key, false));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.funder.key,
            true,
        ));
        accounts.push(solana_instruction::AccountMeta::new(*self.payer.key, true));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.mint.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.token_program.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let mut data = FundDividendsInstructionData::new().try_to_vec().unwrap();
        let mut args = self.__args.try_to_vec().unwrap();
        data.append(&mut args);

        let instruction = solana_instruction::Instruction {
            program_id: crate::DIVIDENDS_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(8 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.distributor.clone());
        account_infos.push(self.from.clone());
        account_infos.push(self.to.clone());
        account_infos.push(self.funder.clone());
        account_infos.push(self.payer.clone());
        account_infos.push(self.mint.clone());
        account_infos.push(self.token_program.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `FundDividends` via CPI.
///
/// ### Accounts:
///
///   0. `[writable]` distributor
///   1. `[writable]` from
///   2. `[writable]` to
///   3. `[signer]` funder
///   4. `[writable, signer]` payer
///   5. `[]` mint
///   6. `[]` token_program
#[derive(Clone, Debug)]
pub struct FundDividendsCpiBuilder<'a, 'b> {
    instruction: Box<FundDividendsCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> FundDividendsCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(FundDividendsCpiBuilderInstruction {
            __program: program,
            distributor: None,
            from: None,
            to: None,
            funder: None,
            payer: None,
            mint: None,
            token_program: None,
            amount: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    /// The [MerkleDistributor].
    #[inline(always)]
    pub fn distributor(
        &mut self,
        distributor: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.distributor = Some(distributor);
        self
    }
    /// Account which send the funding tokens.
    #[inline(always)]
    pub fn from(&mut self, from: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.from = Some(from);
        self
    }
    /// Distributor ATA containing the tokens to distribute.
    #[inline(always)]
    pub fn to(&mut self, to: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.to = Some(to);
        self
    }
    /// Who is funding the tokens.
    #[inline(always)]
    pub fn funder(&mut self, funder: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.funder = Some(funder);
        self
    }
    /// Payer of the fund dividends.
    #[inline(always)]
    pub fn payer(&mut self, payer: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.payer = Some(payer);
        self
    }
    #[inline(always)]
    pub fn mint(&mut self, mint: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.mint = Some(mint);
        self
    }
    /// SPL [Token] program.
    #[inline(always)]
    pub fn token_program(
        &mut self,
        token_program: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.token_program = Some(token_program);
        self
    }
    #[inline(always)]
    pub fn amount(&mut self, amount: u64) -> &mut Self {
        self.instruction.amount = Some(amount);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let args = FundDividendsInstructionArgs {
            amount: self.instruction.amount.clone().expect("amount is not set"),
        };
        let instruction = FundDividendsCpi {
            __program: self.instruction.__program,

            distributor: self
                .instruction
                .distributor
                .expect("distributor is not set"),

            from: self.instruction.from.expect("from is not set"),

            to: self.instruction.to.expect("to is not set"),

            funder: self.instruction.funder.expect("funder is not set"),

            payer: self.instruction.payer.expect("payer is not set"),

            mint: self.instruction.mint.expect("mint is not set"),

            token_program: self
                .instruction
                .token_program
                .expect("token_program is not set"),
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct FundDividendsCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    distributor: Option<&'b solana_account_info::AccountInfo<'a>>,
    from: Option<&'b solana_account_info::AccountInfo<'a>>,
    to: Option<&'b solana_account_info::AccountInfo<'a>>,
    funder: Option<&'b solana_account_info::AccountInfo<'a>>,
    payer: Option<&'b solana_account_info::AccountInfo<'a>>,
    mint: Option<&'b solana_account_info::AccountInfo<'a>>,
    token_program: Option<&'b solana_account_info::AccountInfo<'a>>,
    amount: Option<u64>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
