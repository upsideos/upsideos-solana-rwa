//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;

pub const ACCEPT_RECLAIMER_OWNERSHIP_DISCRIMINATOR: [u8; 8] = [211, 214, 76, 233, 74, 143, 206, 28];

/// Accounts.
#[derive(Debug)]
pub struct AcceptReclaimerOwnership {
    /// Reclaimer account storing the wallet address.
    pub reclaimer: solana_pubkey::Pubkey,
    /// Access Control for Security Token.
    pub access_control: solana_pubkey::Pubkey,

    pub security_mint: solana_pubkey::Pubkey,
    /// New owner signing the transaction (must match proposed_wallet_address).
    pub new_owner: solana_pubkey::Pubkey,
    /// The [System] program.
    pub system_program: solana_pubkey::Pubkey,
}

impl AcceptReclaimerOwnership {
    pub fn instruction(&self) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(&[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(5 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(self.reclaimer, false));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.access_control,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.security_mint,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.new_owner,
            true,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.system_program,
            false,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let data = AcceptReclaimerOwnershipInstructionData::new()
            .try_to_vec()
            .unwrap();

        solana_instruction::Instruction {
            program_id: crate::DIVIDENDS_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct AcceptReclaimerOwnershipInstructionData {
    discriminator: [u8; 8],
}

impl AcceptReclaimerOwnershipInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [211, 214, 76, 233, 74, 143, 206, 28],
        }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for AcceptReclaimerOwnershipInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

/// Instruction builder for `AcceptReclaimerOwnership`.
///
/// ### Accounts:
///
///   0. `[writable]` reclaimer
///   1. `[]` access_control
///   2. `[]` security_mint
///   3. `[signer]` new_owner
///   4. `[optional]` system_program (default to `11111111111111111111111111111111`)
#[derive(Clone, Debug, Default)]
pub struct AcceptReclaimerOwnershipBuilder {
    reclaimer: Option<solana_pubkey::Pubkey>,
    access_control: Option<solana_pubkey::Pubkey>,
    security_mint: Option<solana_pubkey::Pubkey>,
    new_owner: Option<solana_pubkey::Pubkey>,
    system_program: Option<solana_pubkey::Pubkey>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl AcceptReclaimerOwnershipBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    /// Reclaimer account storing the wallet address.
    #[inline(always)]
    pub fn reclaimer(&mut self, reclaimer: solana_pubkey::Pubkey) -> &mut Self {
        self.reclaimer = Some(reclaimer);
        self
    }
    /// Access Control for Security Token.
    #[inline(always)]
    pub fn access_control(&mut self, access_control: solana_pubkey::Pubkey) -> &mut Self {
        self.access_control = Some(access_control);
        self
    }
    #[inline(always)]
    pub fn security_mint(&mut self, security_mint: solana_pubkey::Pubkey) -> &mut Self {
        self.security_mint = Some(security_mint);
        self
    }
    /// New owner signing the transaction (must match proposed_wallet_address).
    #[inline(always)]
    pub fn new_owner(&mut self, new_owner: solana_pubkey::Pubkey) -> &mut Self {
        self.new_owner = Some(new_owner);
        self
    }
    /// `[optional account, default to '11111111111111111111111111111111']`
    /// The [System] program.
    #[inline(always)]
    pub fn system_program(&mut self, system_program: solana_pubkey::Pubkey) -> &mut Self {
        self.system_program = Some(system_program);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = AcceptReclaimerOwnership {
            reclaimer: self.reclaimer.expect("reclaimer is not set"),
            access_control: self.access_control.expect("access_control is not set"),
            security_mint: self.security_mint.expect("security_mint is not set"),
            new_owner: self.new_owner.expect("new_owner is not set"),
            system_program: self
                .system_program
                .unwrap_or(solana_pubkey::pubkey!("11111111111111111111111111111111")),
        };

        accounts.instruction_with_remaining_accounts(&self.__remaining_accounts)
    }
}

/// `accept_reclaimer_ownership` CPI accounts.
pub struct AcceptReclaimerOwnershipCpiAccounts<'a, 'b> {
    /// Reclaimer account storing the wallet address.
    pub reclaimer: &'b solana_account_info::AccountInfo<'a>,
    /// Access Control for Security Token.
    pub access_control: &'b solana_account_info::AccountInfo<'a>,

    pub security_mint: &'b solana_account_info::AccountInfo<'a>,
    /// New owner signing the transaction (must match proposed_wallet_address).
    pub new_owner: &'b solana_account_info::AccountInfo<'a>,
    /// The [System] program.
    pub system_program: &'b solana_account_info::AccountInfo<'a>,
}

/// `accept_reclaimer_ownership` CPI instruction.
pub struct AcceptReclaimerOwnershipCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,
    /// Reclaimer account storing the wallet address.
    pub reclaimer: &'b solana_account_info::AccountInfo<'a>,
    /// Access Control for Security Token.
    pub access_control: &'b solana_account_info::AccountInfo<'a>,

    pub security_mint: &'b solana_account_info::AccountInfo<'a>,
    /// New owner signing the transaction (must match proposed_wallet_address).
    pub new_owner: &'b solana_account_info::AccountInfo<'a>,
    /// The [System] program.
    pub system_program: &'b solana_account_info::AccountInfo<'a>,
}

impl<'a, 'b> AcceptReclaimerOwnershipCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: AcceptReclaimerOwnershipCpiAccounts<'a, 'b>,
    ) -> Self {
        Self {
            __program: program,
            reclaimer: accounts.reclaimer,
            access_control: accounts.access_control,
            security_mint: accounts.security_mint,
            new_owner: accounts.new_owner,
            system_program: accounts.system_program,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(5 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(
            *self.reclaimer.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.access_control.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.security_mint.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.new_owner.key,
            true,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.system_program.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let data = AcceptReclaimerOwnershipInstructionData::new()
            .try_to_vec()
            .unwrap();

        let instruction = solana_instruction::Instruction {
            program_id: crate::DIVIDENDS_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(6 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.reclaimer.clone());
        account_infos.push(self.access_control.clone());
        account_infos.push(self.security_mint.clone());
        account_infos.push(self.new_owner.clone());
        account_infos.push(self.system_program.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `AcceptReclaimerOwnership` via CPI.
///
/// ### Accounts:
///
///   0. `[writable]` reclaimer
///   1. `[]` access_control
///   2. `[]` security_mint
///   3. `[signer]` new_owner
///   4. `[]` system_program
#[derive(Clone, Debug)]
pub struct AcceptReclaimerOwnershipCpiBuilder<'a, 'b> {
    instruction: Box<AcceptReclaimerOwnershipCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> AcceptReclaimerOwnershipCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(AcceptReclaimerOwnershipCpiBuilderInstruction {
            __program: program,
            reclaimer: None,
            access_control: None,
            security_mint: None,
            new_owner: None,
            system_program: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    /// Reclaimer account storing the wallet address.
    #[inline(always)]
    pub fn reclaimer(&mut self, reclaimer: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.reclaimer = Some(reclaimer);
        self
    }
    /// Access Control for Security Token.
    #[inline(always)]
    pub fn access_control(
        &mut self,
        access_control: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.access_control = Some(access_control);
        self
    }
    #[inline(always)]
    pub fn security_mint(
        &mut self,
        security_mint: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.security_mint = Some(security_mint);
        self
    }
    /// New owner signing the transaction (must match proposed_wallet_address).
    #[inline(always)]
    pub fn new_owner(&mut self, new_owner: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.new_owner = Some(new_owner);
        self
    }
    /// The [System] program.
    #[inline(always)]
    pub fn system_program(
        &mut self,
        system_program: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.system_program = Some(system_program);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let instruction = AcceptReclaimerOwnershipCpi {
            __program: self.instruction.__program,

            reclaimer: self.instruction.reclaimer.expect("reclaimer is not set"),

            access_control: self
                .instruction
                .access_control
                .expect("access_control is not set"),

            security_mint: self
                .instruction
                .security_mint
                .expect("security_mint is not set"),

            new_owner: self.instruction.new_owner.expect("new_owner is not set"),

            system_program: self
                .instruction
                .system_program
                .expect("system_program is not set"),
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct AcceptReclaimerOwnershipCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    reclaimer: Option<&'b solana_account_info::AccountInfo<'a>>,
    access_control: Option<&'b solana_account_info::AccountInfo<'a>>,
    security_mint: Option<&'b solana_account_info::AccountInfo<'a>>,
    new_owner: Option<&'b solana_account_info::AccountInfo<'a>>,
    system_program: Option<&'b solana_account_info::AccountInfo<'a>>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
